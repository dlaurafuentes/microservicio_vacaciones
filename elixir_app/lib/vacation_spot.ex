defmodule VacationSpot do
  def process_data(data) do
    # Lógica compleja en Elixir (ej: IA, cálculos distribuidos)
    reversed_data = String.reverse(data)
    %{result: reversed_data, processed_by: "Elixir"}
  end
end